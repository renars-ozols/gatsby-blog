import styled from "styled-components"

export const PageContainer = styled.div`
  margin: 0 auto;
  max-width: 144rem; /*Desktop brakepoint */
  background-color: ${props => props.theme.colors.white};
`
